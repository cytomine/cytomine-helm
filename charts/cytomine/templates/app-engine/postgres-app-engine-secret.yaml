apiVersion: v1
kind: Secret
metadata:
  name: "postgres-app-engine-secret"
type: Opaque
data:
  {{- $secretObj := (lookup "v1" "Secret" .Release.Namespace "postgres-app-engine-secret") | default dict }}
  {{- $secretData := (get $secretObj "data") | default dict }}
  {{- $secret := (get $secretData "password") | default (randAlphaNum 64 | b64enc) }}
  password: {{ $secret | quote }}
